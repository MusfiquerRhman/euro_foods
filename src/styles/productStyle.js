import { makeStyles } from "@mui/styles"

const style = makeStyles(() => ({
    input: {
        border: "1px solid #00B3A6"
    },
    offer: {
        padding: "0 1rem"
    }
}))

export default style;